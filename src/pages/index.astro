---
import BaseLayout from "../layouts/BaseLayout.astro";
import site from "../data/site.json";
import quickLinks from "../data/quick-links.json";
import events from "../data/events.json";
import resources from "../data/resources.json";
import officers from "../data/officers.json";

const externalLinkAttrs = { target: "_blank", rel: "noreferrer" };
---

<BaseLayout>
  <a class="skip-link" href="#main">{site.skipLinkText}</a>

  <header class="site-header">
    <a class="brand" href="#home" aria-label={`${site.title} home`}>
      <img src="/assets/images/third-sophistic.png" alt="" width="48" height="48" />
      <span>
        <strong>{site.title}</strong>
        <small>{site.subtitle}</small>
      </span>
    </a>
    <button class="nav-toggle" type="button" aria-expanded="false" aria-controls="site-nav">
      <span></span>
      <span></span>
      <span></span>
    </button>
    <nav class="site-nav" id="site-nav" aria-label="Primary navigation">
      <a href="#events">{site.navEventsText}</a>
      <a href="#resources">{site.navResourcesText}</a>
      <a href="#officers">{site.navOfficersText}</a>
      <a href="#about">{site.navAboutText}</a>
      <a href="#contact">{site.navContactText}</a>
    </nav>
  </header>

  <main id="main">
    <section class="hero" id="home" aria-labelledby="hero-title">
      <div class="hero-copy">
        <p class="eyebrow">{site.eyebrow}</p>
        <h1 id="hero-title">{site.heroTitle}</h1>
        <p class="dek">{site.heroText}</p>
        <div class="hero-actions">
          <a class="button primary" href={site.primaryActionUrl}>{site.primaryActionText}</a>
          <a class="button" href={site.secondaryActionUrl}>{site.secondaryActionText}</a>
        </div>
      </div>

      <aside class="status-board" aria-label="Student quick links">
        <div class="board-heading">
          <span>{site.studentDeskLabel}</span>
          <strong>{site.studentDeskTerm}</strong>
        </div>
        {
          quickLinks.items.map((link) => (
            <a href={link.url} {...externalLinkAttrs}>
              <span>{link.label}</span>
              <strong>{link.title}</strong>
            </a>
          ))
        }
      </aside>
    </section>

    <section class="section next-up" id="events" aria-labelledby="events-title">
      <div class="section-kicker">{site.eventsKicker}</div>
      <div class="section-heading">
        <h2 id="events-title">{site.eventsTitle}</h2>
        <p>{site.eventsText}</p>
      </div>

      <div class="event-strip" aria-label="Featured events">
        {
          events.items.map((event) => (
            <article class="event-card">
              <time datetime={event.datetime}>{event.date}</time>
              <h3>{event.title}</h3>
              <p>{event.description}</p>
              <span>{event.status}</span>
            </article>
          ))
        }
      </div>
    </section>

    <section class="section resource-section" id="resources" aria-labelledby="resources-title">
      <div class="section-kicker">{site.resourcesKicker}</div>
      <div class="section-heading">
        <h2 id="resources-title">{site.resourcesTitle}</h2>
        <p>{site.resourcesText}</p>
      </div>

      <div class="resource-grid">
        {
          resources.items.map((resource) => {
            const isExternal = resource.url.startsWith("http");
            return (
              <a
                class:list={["resource-link", resource.style === "muted" && "muted-link"]}
                href={resource.url}
                {...(isExternal ? externalLinkAttrs : {})}
              >
                <span>{resource.label}</span>
                <strong>{resource.title}</strong>
                <p>{resource.description}</p>
              </a>
            );
          })
        }
      </div>
    </section>

    <section class="section officers-section" id="officers" aria-labelledby="officers-title">
      <div class="section-kicker">{site.officersKicker}</div>
      <div class="section-heading">
        <h2 id="officers-title">{site.officersTitle}</h2>
        <p>{site.officersText}</p>
      </div>

      <div class="officer-layout">
        {
          officers.items.map((officer) => (
            <article class="officer-card">
              <span>{officer.role}</span>
              <h3>{officer.name}</h3>
              <p>{officer.description}</p>
            </article>
          ))
        }
      </div>
    </section>

    <section class="section about-section" id="about" aria-labelledby="about-title">
      <div class="section-kicker">{site.aboutKicker}</div>
      <div class="about-copy">
        <h2 id="about-title">{site.aboutTitle}</h2>
        {site.aboutParagraphs.map((paragraph) => <p>{paragraph}</p>)}
      </div>
    </section>

    <section class="section contact-section" id="contact" aria-labelledby="contact-title">
      <div class="section-kicker">{site.contactKicker}</div>
      <div class="contact-panel">
        <div>
          <h2 id="contact-title">{site.contactTitle}</h2>
          <p>{site.contactText}</p>
        </div>
        <div class="contact-actions">
          <a class="button primary" href={`mailto:${site.contactEmail}`}>{site.contactEmailButtonText}</a>
          <a class="button" href={site.socialUrl} {...externalLinkAttrs}>{site.contactSocialButtonText}</a>
        </div>
      </div>
    </section>
  </main>

  <footer class="site-footer">
    <span>{site.title}</span>
    <span>{site.footerText}</span>
  </footer>
</BaseLayout>
